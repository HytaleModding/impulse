# impulse-examples

Impulse examples to help developers integrate the framework in their mod.

Fresh worlds need an explicit physics space before example commands can spawn bodies:

```bash
/impulse space create --backend=impulse:rapier
```